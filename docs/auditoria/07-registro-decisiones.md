# 07 - Registro de decisiones tecnicas

Este archivo conserva decisiones tomadas durante y despues de la auditoria. No reemplaza ADRs formales si el equipo decide crearlos; funciona como registro inicial trazable.

| Fecha | Decision | Contexto | Alternativas consideradas | Motivo de la decision | Impacto | Estado |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-01 | Crear documentacion persistente de auditoria | La auditoria inicial estaba en la conversacion y podia perderse o quedar desconectada del repo. | Mantener solo respuesta conversacional; crear issues externos; crear docs en repo. | El repositorio debe conservar diagnostico, plan y criterios de aceptacion. | La correccion futura puede referenciar IDs y archivos estables. | Aprobada |
| 2026-07-01 | No modificar codigo durante la fase de documentacion | El objetivo actual es documentar, no corregir. | Corregir bloqueantes inmediatamente; documentar primero. | Separar diagnostico/documentacion de cambios funcionales reduce riesgo y facilita revision. | Solo se permiten archivos Markdown bajo `docs/auditoria/`. | Aprobada |
| 2026-07-01 | Usar IDs unicos para trazabilidad de hallazgos | Los hallazgos cubren seguridad, datos, arquitectura, performance, UX, testing, produccion y funcionalidades faltantes. | IDs secuenciales globales; IDs por categoria; sin IDs. | Los IDs por categoria facilitan busqueda, backlog, tests y PRs. | Cada tarea y prueba puede referenciar un hallazgo concreto. | Aprobada |
| 2026-07-01 | No eliminar hallazgos corregidos, solo cambiar estado | La auditoria debe conservar historial de riesgos y correcciones. | Borrar hallazgos al corregir; moverlos a changelog; mantenerlos con estado. | Mantenerlos permite auditoria posterior y evita perder contexto. | `02-hallazgos.md` debe actualizar `Estado`, no borrar secciones. | Aprobada |
| 2026-07-01 | Priorizar consistencia financiera e inventario antes de nuevas funcionalidades | La auditoria encontro riesgos P0 en pagos, utilidad, stock, lotes e incidencias. | Completar UI pendiente primero; optimizar performance primero; corregir datos primero. | Los errores de datos pueden generar informacion financiera y operativa falsa. | El roadmap inicia por P0 de datos y seguridad. | Propuesta |
| 2026-07-01 | Tratar produccion como bloqueada hasta resolver P0/P1 | El sistema contiene hallazgos criticos no corregidos. | Deploy parcial; beta interna; no deploy. | Un deploy con datos reales podria corromper stock/utilidad o exponer recibos. | Produccion requiere checklist y aceptacion explicita de riesgos. | Propuesta |
| 2026-07-01 | Permitir lectura directa de settings solo fuera del runtime Next | Las pruebas de dominio Playwright importan helpers que llaman `getSettings()`, pero `unstable_cache` requiere `incrementalCache` de Next. | Reescribir los E2E para no importar dominio; mockear settings; fallback directo condicionado al error de runtime. | El fallback mantiene `unstable_cache` en la app y habilita scripts/E2E sin introducir cache en proceso. | `lib/settings.ts` cae a DB directa solo ante `incrementalCache missing`; la invalidacion por tags sigue aplicando en runtime Next. | Aprobada |
| 2026-07-03 | Resolver pagos pendientes multi-pedido al cerrar reservas | `AUD-DATA-013` podia dejar pagos pendientes aplicados a pedidos vencidos/cancelados. | Rechazar siempre el pago; dejarlo pendiente; eliminar solo la aplicacion del pedido cerrado. | Rechazar siempre podria destruir fondos validos aplicados a otros pedidos; eliminar solo la aplicacion preserva el pago para pedidos activos. | Pagos con una sola aplicacion al pedido cerrado se rechazan; pagos multi-pedido quedan pendientes con la aplicacion cerrada eliminada. | Aprobada |
| 2026-07-03 | Cancelar incidencias revierte efectos salvo creditos ya usados | `AUD-DATA-002` requeria una regla clara para stock y creditos. | Revertir siempre; bloquear siempre; revertir stock y bloquear solo creditos aplicados. | Mantiene consistencia de stock y evita crear saldos negativos o deudas implicitas por creditos ya usados. | `cancelIncident()` revierte RESTOCK y DAMAGE/LOSS propios; anula creditos no usados; bloquea creditos aplicados. | Aprobada |
| 2026-07-03 | Reducir vida del JWT a 15 minutos | `AUD-SEC-001` requeria invalidar usuarios desactivados o degradados dentro de una ventana definida sin introducir schema nuevo. | `tokenVersion/sessionVersion`; revalidar usuario en cada request; reducir `maxAge`. | Es el cambio mas pequeno y evita una consulta DB por request, aceptando una ventana maxima acotada. | `session.maxAge` y `jwt.maxAge` quedan en 900 segundos; cambios de rol/isActive se reflejan al expirar la sesion. | Aprobada |
| 2026-07-03 | Mantener `DISPATCH` sin lectura general de clientes/pedidos | `AUD-SEC-005`, `AUD-UX-002` y `AUD-UX-003` requerian resolver enlaces rotos sin ampliar exposicion de datos. | Ampliar permisos de `DISPATCH` a pedidos/clientes; quitar dashboard de despacho; crear loaders acotados al flujo de envios. | Es el cambio minimo seguro: despacho puede crear y gestionar envios, pero no navegar datos comerciales fuera del contexto de envio. | Dashboard de despacho solo enlaza a rutas de envios; `/envios/nuevo` precarga pedidos elegibles mediante action propia de envios. | Aprobada |
| 2026-07-03 | Servir recibos por endpoint autenticado y mantener productos publicos | `AUD-SEC-003` requeria proteger comprobantes sin romper imagenes publicas del catalogo. | Hacer todo Blob privado; URLs firmadas temporales en UI; endpoint autenticado por recibo. | El endpoint reduce exposicion de URLs, preserva UI simple y permite compatibilidad con recibos historicos mientras los nuevos se suben privados. | `uploadImage()` conserva default publico para productos; pagos pasan `{ access: "private" }`; `/api/payment-receipts/[id]` valida rol y streaméa Blob. | Aprobada |
| 2026-07-03 | Proteger stock comprometido como invariante minimo | `AUD-DATA-005` y `AUD-DATA-006` mostraban operaciones manuales que podian consumir reservas o unidades vendidas. | Validar solo stock no negativo; mover todo a lotes antes; validar `stock >= reservedStock + soldStock` ahora. | Es la correccion minima compatible con el modelo actual mientras `AUD-DATA-004` define la fuente final por lotes. | Ajustes manuales e incidencias de inventario propio no pueden dejar stock total por debajo de reservas y ventas. | Aprobada |
| 2026-07-03 | Prorratear descuento/envio en la creacion de la venta | `AUD-DATA-007` mostraba utilidad sobreestimada por snapshots de linea que no descontaban el descuento del pedido. | Prorratear solo en `recognizeOrderProfit`; prorratear al crear la venta; ignorar descuento. | Prorratear al crear la venta garantiza utilidad coherente desde `PAYMENT_VALIDATION_PENDING` y evita recalcular despues. | `persistQuickSaleLine()` persiste `lineDiscountPen` y `netLineRevenuePen`; `lib/sales.ts` reparte con `distributeOrderDiscount` antes de iterar. | Aprobada |
| 2026-07-03 | Opcion B para stock de lotes vs variante | `AUD-DATA-004` pedia una sola fuente de verdad entre `ProductVariant.stock` y `ImportBatchItem.quantityAvailable`. | Opcion A (lotes como fuente unica, sin schema migration); opcion C (reconciliacion sin tocar paths). | Opcion B mantiene `ProductVariant.stock` como proyeccion sincronizada en la misma transaccion. Es la menor invasion con mayor reversibilidad, no requiere migraciones Prisma (`AUD-DATA-012` sigue pendiente) y habilita una transicion gradual a la opcion A en el futuro. | `lib/stock-sync.ts` centraliza `applyBatchStockDelta()` y assert de invariante; `actions/import-batches.ts` y `lib/order-batch-allocation.ts` sincronizan delta; `scripts/reconcile-variant-stock.ts` corrige drift residual. | Aprobada |

## Reglas para futuras decisiones

* Registrar decisiones que cambien prioridad, alcance o criterio de aceptacion de hallazgos.
* Si se difiere un hallazgo P0 o P1, registrar decision con motivo, mitigacion y responsable.
* Si se cambia el modelo de datos de stock, lotes, pagos o creditos, agregar decision antes de implementar.
* Mantener fechas en formato `YYYY-MM-DD`.
